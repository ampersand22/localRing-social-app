{
  "dependencies": {
    "@apollo/client": "^3.8.5",
    "@apollo/server": "^4.9.4",
    "@graphql-tools/schema": "^10.0.0",
    "@prisma/client": "^5.4.1",
    "@sendgrid/mail": "^7.7.0",
    "apollo-server": "^3.12.1",
    "apollo-server-core": "^3.12.1",
    "apollo-server-express": "^3.12.1",
    "axios": "^1.5.1",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.2",
    "colors": "^1.4.0",
    "connect-redis": "^7.1.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express-session": "^1.17.3",
    "generate-password": "^1.7.0",
    "graphql": "^16.8.1",
    "graphql-middleware": "^6.1.35",
    "graphql-scalars": "^1.22.2",
    "graphql-ws": "^5.14.1",
    "helmet": "^7.0.0",
    "http": "^0.0.1-security",
    "ioredis": "^5.3.2",
    "jsonwebtoken": "^9.0.2",
    "jwt-decode": "^3.1.2",
    "lodash": "^4.17.21",
    "mongoose": "^7.5.4",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nanoid": "^5.0.1",
    "nexus": "^1.3.0",
    "ramda": "^0.29.1",
    "typeorm": "^0.3.17",
    "uuid": "^9.0.1",
    "ws": "^8.14.2"
  },
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "start": "npm run compile && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "lint": "eslint --ext .ts,.tsx,.js,.jsx src tests",
    "build": "npm -s run clean && npm -s run generate && tsc && cp -r files dist/ && cp -r html dist/",
    "tsc": "tsc",
    "generate": "yarn generate:prisma && yarn generate:nexus",
    "generate:prisma": "prisma generate",
    "generate:nexus": "ts-node --transpile-only src/schema",
    "generate:local": "dotenv -e ./dotenv/.env -- yarn generate:prisma && dotenv -e ./dotenv/.env -- yarn generate:nexus",
    "generate:test": "npm -s run generate:prisma:test && npm -s run generate:nexus:test",
    "generate:prisma:test": "dotenv -e ./dotenv/test.env -- prisma generate",
    "generate:nexus:test": "dotenv -e ./dotenv/test.env  -- ts-node --transpile-only src/schema"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^20.8.2",
    "typescript": "^5.2.2"
  }
}
